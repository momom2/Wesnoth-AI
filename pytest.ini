[pytest]
# Collect only the project's own tests. kaggle/kaggle_output/ holds
# artifacts pulled from Kaggle runs -- the first Phase-1 notebook
# version accidentally shipped a whole repo clone back, and its
# test_*.py copies collided with the real ones at collection time
# ("import file mismatch"). wesnoth_src/ and the venv are just
# collection noise. .claude/ holds other sessions' git worktrees, whole
# checkouts whose tests collide with these the same way.
norecursedirs = kaggle wesnoth_src .venv venv .git __pycache__ build dist node_modules .claude

# Two-tier suite (2026-07-18). The default run EXCLUDES tests marked
# `slow` -- full-game / subprocess / threading e2e tests measured at
# >10s each.
#
#   pytest            -> fast tier (run locally after every change)
#   pytest -m ""      -> EVERYTHING; CI runs it on every push
#                        (.github/workflows/tests.yml)
#   pytest -m slow    -> slow tier only
#
# The slow tests are regression guards for exactly the bugs that
# otherwise surface six hours into a training run. The laptop does not
# run them (they play self-play games); CI does, so a branch merges on
# a green CI run and a training campaign launches from a commit that
# has one (CLAUDE.md, Testing).
#
# `-rfEs` lists every failed, errored and skipped test with its location
# and reason at the end of the run (2026-09-23). The `s` is the addition:
# a test that skips in some runs and passes in others is a coverage hole
# that a bare count hides (41 vs 42 skipped was seen across runs, and no
# quiet run reproduced it), so the next one has to name itself. `fE` is
# pytest's own default, restated because any `-r` REPLACES the default:
# a bare `-rs` dropped the FAILED lines from the summary, which the first
# CI run showed.
addopts = -m "not slow" -rfEs
markers =
    slow: full-game / subprocess / threading e2e (>10s); excluded from the default run -- see pytest.ini header
